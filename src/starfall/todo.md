# Starfall TODO

Чек-лист задач, необходимых для полноценной реализации базовой игры.

## 1. Критические проблемы

- [ ] Инициализировать `GameEntity.players` пустым массивом до вызова
      `this.players.push(...)`.
      Файл: `entities/game.entity.ts`.

- [ ] Исправить создание стартовой колоды. Сейчас второй вызов
      `deck.updateCards()` заменяет добавленных штурмовиков разведчиками.
      Стартовая колода каждого игрока должна содержать:
  - 8 `Scout`;
  - 2 `Trooper`.

- [x] Разделить карты до заполнения торгового ряда:
  - 80 карт торговой колоды;
  - 10 `Explorer`;
  - стартовые карты игроков.

- [x] Не определять назначение карты только по имени. Добавить в описание карты
      признак наподобие:

  ```ts
  deck: 'trade' | 'explorer' | 'starter';
  ```

- [ ] Запретить повторный розыгрыш одной карты. Сейчас повторный вызов
      `playCard(id)` снова начисляет её торговлю и бой.

- [x] Реализовать отдельный розыгрыш баз:
  - удалять базу с руки;
  - добавлять её в `player.bases`;
  - оставлять между ходами;
  - активировать её основной эффект каждый ход;
  - учитывать при проверке союзных способностей;
  - не отправлять в сброс вместе с кораблями.

- [x] Проверять количество торговли перед покупкой карты.

- [x] Не допускать отрицательный баланс торговли.

- [ ] Проверять, что покупаемая карта действительно находится в торговом ряду
      или стопке исследователей.

- [ ] Добавить возможность покупать `Explorer`.

- [ ] Корректно обрабатывать окончание торговой колоды. Не добавлять
      `undefined` в торговый ряд, если новой карты нет.

## 2. Модель хода

- [ ] Добавить статус партии:

  ```ts
  type GameStatus = 'waiting' | 'active' | 'finished';
  ```

- [ ] Добавить номер или индекс текущего игрока.

- [ ] Добавить номер хода.

- [ ] Хранить и назначать единственного победителя партии через `isWinner`:
  - при создании партии устанавливать обоим игрокам `isWinner = false`;
  - разрешать назначить победителем только участника этой партии;
  - перед назначением сбрасывать `isWinner` у обоих игроков;
  - устанавливать `isWinner = true` только выбранному победителю;
  - назначать победителя и переводить партию в `finished` одной атомарной
    операцией `GameEntity.finishGame(...)`;
  - не допускать завершённую партию без победителя;
  - не допускать победителя у партии в статусе `waiting` или `active`;
  - запрещать повторное назначение или замену победителя завершённой партии;
  - не позволять контроллеру или сервису изменять `isWinner` напрямую;
  - сохранять и восстанавливать `isWinner` из БД.

- [ ] Реализовать `startGame()`.

- [ ] Реализовать `endTurn()`.

- [ ] Вызывать `PlayerEntity.startTurn()` только из контролируемого перехода
      хода в `GameEntity` и не допускать повторной активации начала одного хода.

- [ ] Проверять, что действие выполняет текущий игрок.

- [ ] Запрещать игровые действия после завершения партии.

- [ ] Реализовать полный цикл хода:
  1. начало хода;
  2. активация постоянных баз;
  3. розыгрыш карт;
  4. использование способностей;
  5. покупки и атаки;
  6. сброс неиспользованных карт;
  7. сброс разыгранных кораблей;
  8. обнуление торговли и боя;
  9. добор новой руки;
  10. переход хода.

- [ ] В `finishTurn()` обнулять торговлю и бой.

- [ ] Первый игрок в своём первом ходу должен получить 3 карты, второй — 5.

- [ ] Корректно обрабатывать добор, если одновременно закончились личная колода
      и стопка сброса.

## 3. Зоны карт

- [ ] Добавить отдельную зону для разыгранных кораблей:

  ```ts
  playedShips: DeckEntity;
  ```

- [ ] Использовать `playedShips` для:
  - подсчёта разыгранных фракций;
  - союзных способностей;
  - `Stealth Needle`;
  - `Fleet HQ`;
  - `Blob World`;
  - отображения игрового стола;
  - сброса кораблей в конце хода.

- [ ] При очистке зоны разыгранных карт сохранять существующий экземпляр
      `DeckEntity` через `currentPlayedCards.updateCards([])`, а не заменять его
      новым объектом. Это не позволит внешним ссылкам на зону устареть.

- [ ] Добавить отдельную зону для утилизированных карт:

  ```ts
  scrappedCards: DeckEntity;
  ```

- [ ] Различать:
  - утилизацию карты с руки;
  - утилизацию карты из сброса;
  - утилизацию самой разыгранной карты;
  - утилизацию карты торгового ряда;
  - уничтожение базы с перемещением в сброс.

## 4. Союзные способности

- [ ] Реализовать `CardEntity.useMatchAbility()`.

- [ ] Союзная способность должна становиться доступна при наличии другой карты
      той же фракции.

- [ ] Порядок розыгрыша карт не должен мешать активации союза.

- [ ] Базы, оставшиеся с предыдущих ходов, должны считаться союзниками.

- [ ] Карта не должна активировать собственную союзную способность сама.

- [ ] `Mech World` должен считаться союзником всех фракций.

- [ ] `Stealth Needle` должен копировать свойства и фракцию выбранного корабля.

- [ ] Союзный эффект одной карты должен активироваться не более одного раза за
      ход.

- [ ] После розыгрыша карты пересчитывать доступность союзных способностей всех
      разыгранных карт и баз текущего игрока.

## 5. Идентификация и использование способностей

- [ ] Не идентифицировать использованную способность только через
      `TCardAbilitiesNames`.

- [ ] Добавить идентификатор способности, например:

  ```ts
  type AbilityId = 'primary' | 'ally' | 'scrap';
  ```

  Либо использовать уникальный ID способности внутри экземпляра карты.

- [ ] В `CardEntity.useAbility()` проверять:
  - разыграна ли карта;
  - существует ли способность;
  - доступна ли она;
  - выполнены ли её условия;
  - не использована ли она ранее.

- [ ] После использования устанавливать `ability.isUsed = true`.

- [ ] Не добавлять одинаковые записи в `usedAbilities`.

- [ ] Реализовать вызов:
  - обычных способностей;
  - союзных способностей;
  - утилизационных способностей.

- [ ] Сбрасывать состояние использованных способностей в подходящий момент
      следующего хода.

- [ ] В начале каждого хода снова делать доступными основные и союзные
      способности баз. Эффекты с выбором не выполнять автоматически — игрок
      должен активировать их сам.

- [ ] Разделить состояния «карта находится в игре» и «карта разыграна в текущем
      ходу». Не использовать один `isPlayed` для обоих состояний базы.

## 6. Исполнение `Action`

- [ ] Реализовать получение очков торговли.

- [ ] Реализовать восстановление влияния.

- [ ] Реализовать утилизацию карты с руки.

- [ ] Реализовать утилизацию карты из сброса.

- [ ] Реализовать выбор между рукой и сбросом.

- [ ] Реализовать утилизацию карты торгового ряда с последующим заполнением
      торгового ряда.

- [ ] Реализовать уничтожение выбранной базы.

- [ ] Реализовать `CHOOSE_ONE`.

- [ ] Реализовать `SEQUENCE`.

- [ ] Реализовать условные действия.

- [ ] Реализовать сброс карт и добор такого же количества.

- [ ] Реализовать помещение следующего купленного корабля наверх личной колоды.

- [ ] Реализовать бесплатное приобретение корабля наверх личной колоды.

- [ ] Реализовать копирование корабля.

- [ ] Реализовать универсального союзника всех фракций.

- [ ] Реализовать получение боя за каждый разыгранный корабль.

- [ ] Реализовать добор за карты указанной фракции.

- [ ] Учитывать параметр `value` у сброса карт соперником, а не всегда добавлять
      только единицу.

## 7. Ожидающие выбора действия

Для способностей, требующих ответа игрока, добавить состояние ожидающего
действия:

```ts
type PendingAction =
  | { type: 'choose_card_to_scrap'; /* параметры */ }
  | { type: 'choose_base_to_destroy'; /* параметры */ }
  | { type: 'choose_ship_to_copy'; /* параметры */ }
  | { type: 'choose_one'; /* параметры */ };
```

- [ ] Хранить `pendingAction` в состоянии партии.

- [ ] Не разрешать несовместимые игровые команды, пока обязательный выбор не
      завершён.

- [ ] Проверять, что выбранная карта или база соответствует ограничениям
      способности.

- [ ] Поддержать отмену необязательных действий.

- [ ] Сохранять `pendingAction`, чтобы партия корректно восстанавливалась после
      перезапуска сервера.

## 8. Атака, базы и аванпосты

- [ ] Разрешить атаковать конкретную базу.

- [ ] Хранить или вычислять текущую защиту экземпляра базы.

- [ ] Уничтоженную базу перемещать в сброс владельца, сбрасывая у неё
      `isPlayed` и состояние использованных способностей, чтобы после добора её
      можно было разыграть повторно.

- [ ] Запрещать атаку игрока и обычных баз, пока у него есть аванпост.

- [ ] Разрешать специальным эффектам уничтожать выбранную базу независимо от её
      защиты.

- [ ] Вычитать потраченные очки боя.

- [ ] Не разрешать тратить больше боя, чем накоплено.

- [ ] Не наносить автоматически весь накопленный бой игроку.

- [ ] Завершать игру, когда влияние игрока становится меньше или равно нулю.

- [ ] Не позволять влиянию или защите приводить состояние игры к некорректным
      значениям.

## 9. Сброс карт соперником

- [ ] Применять накопленный `discardCardsCount` в начале следующего хода
      соответствующего игрока.

- [ ] Суммировать несколько эффектов сброса.

- [ ] Позволять сопернику самому выбирать сбрасываемые карты.

- [ ] Создавать обязательный `pendingAction` для выбора карт.

- [ ] Не разрешать продолжить ход, пока обязательный сброс не выполнен.

- [ ] Если карт на руке недостаточно, сбрасывать доступное количество.

- [ ] После выполнения обнулять `discardCardsCount`.

## 10. `DeckEntity`

- [ ] Переименовать `updateCards()` так, чтобы было понятно, что метод полностью
      заменяет содержимое колоды.

- [ ] Добавить явные операции:
  - удалить карту по ID;
  - взять верхнюю карту;
  - положить карту наверх;
  - положить карту в сброс;
  - проверить наличие карты;
  - безопасно заменить карту;
  - переместить карту между зонами.

- [ ] Сделать перемещение карты между зонами атомарной операцией, чтобы один
      экземпляр не оказался сразу в двух местах.

- [ ] Заменить `sort(() => Math.random() - 0.5)` на алгоритм Fisher–Yates.

- [ ] Предусмотреть внедряемый или seed-based генератор случайных чисел для
      воспроизводимых тестов.

## 11. Сохранение состояния

- [ ] Добавить в `CardFromDb`:
  - владельца карты;
  - зону карты;
  - позицию внутри зоны;
  - использованные способности;
  - временное состояние базы;
  - состояние копирования;
  - признак утилизации.

- [ ] Добавить в состояние игрока:
  - разыгранные корабли;
  - базы;
  - ожидающие обязательные действия;
  - порядок хода;
  - состояние первого хода.

- [ ] Добавить модели Prisma для Starfall:
  - партия;
  - участники;
  - экземпляры карт;
  - расположение и порядок карт;
  - текущее состояние партии;
  - победитель;
  - ожидающее действие;
  - версию состояния.

- [ ] Добавить транзакционное сохранение всей игровой команды.

- [ ] Добавить версию состояния или optimistic locking для защиты от
      параллельных запросов.

## 12. API и безопасность состояния

- [ ] Реализовать команды API:
  - создать партию;
  - присоединиться к партии;
  - начать партию;
  - получить состояние;
  - разыграть карту;
  - использовать способность;
  - разрешить ожидающий выбор;
  - купить карту;
  - атаковать базу;
  - атаковать игрока;
  - закончить ход.

- [ ] На каждой команде проверять:
  - личность игрока;
  - участие игрока в партии;
  - текущий ход;
  - статус партии;
  - ожидающее действие;
  - версию состояния;
  - допустимость цели.

- [ ] Не возвращать игроку:
  - руку соперника;
  - порядок карт в колоде соперника;
  - порядок собственной закрытой колоды.

- [ ] Создать отдельные DTO для публичного и приватного представления партии.

- [ ] Предусмотреть идемпотентность команд или защиту от повторного запроса.

## 13. Тесты

- [ ] Проверить правильный состав стартовых колод.

- [ ] Проверить добор трёх карт первым игроком и пяти вторым.

- [ ] Проверить отсутствие стартовых карт и исследователей в торговом ряду.

- [ ] Запретить покупку при недостатке торговли.

- [ ] Запретить повторный розыгрыш одной карты.

- [ ] Проверить сохранение баз между ходами.

- [ ] Проверить перемещение кораблей в сброс в конце хода.

- [ ] Проверить союзную способность при разном порядке розыгрыша карт.

- [ ] Проверить `Mech World` для каждой фракции.

- [ ] Проверить копирование через `Stealth Needle`.

- [ ] Проверить блокировку целей аванпостом.

- [ ] Проверить перемещение утилизированной карты из игры.

- [ ] Проверить перемешивание сброса после окончания личной колоды.

- [ ] Проверить корректное уменьшение торгового ряда после окончания торговой
      колоды.

- [ ] Проверить завершение игры при влиянии меньше или равном нулю.

- [ ] Проверить восстановление партии из базы данных.

- [ ] Проверить, что повторный API-запрос не применяет команду дважды.

## Рекомендуемый порядок реализации

1. Исправить создание партии и разделение колод.
2. Ввести явные зоны карт и безопасное перемещение между ними.
3. Реализовать жизненный цикл хода.
4. Реализовать базы, аванпосты и атаку.
5. Добавить идентификаторы способностей и союзную механику.
6. Добавить `pendingAction`.
7. Реализовать все типы `Action`.
8. Добавить сохранение в Prisma.
9. Добавить API и DTO представления состояния.
10. Покрыть доменную модель и API тестами.
