# Starfall TODO

Чек-лист задач, необходимых для полноценной реализации базовой игры.

## 1. Критические проблемы

- [x] Инициализировать `GameEntity.players` пустым массивом до вызова
      `this.players.push(...)`.
      Файл: `entities/game.entity.ts`.

- [x] Исправить создание стартовой колоды. Сейчас второй вызов
      `deck.updateCards()` заменяет добавленных штурмовиков разведчиками.
      Стартовая колода каждого игрока должна содержать:

  - 8 `Scout`;
  - 2 `Trooper`.

- [x] Разделить карты до заполнения торгового ряда:

  - 80 карт торговой колоды;
  - 10 `Explorer`;
  - стартовые карты игроков.

- [x] Не определять назначение карты только по имени. Добавить в описание карты
      признак наподобие:

  ```ts
  deck: 'trade' | 'explorer' | 'starter';
  ```

- [x] Запретить повторный розыгрыш одной карты. Сейчас повторный вызов
      `playCard(id)` снова начисляет её торговлю и бой.

- [x] Реализовать отдельный розыгрыш баз:

  - удалять базу с руки;
  - добавлять её в `player.bases`;
  - оставлять между ходами;
  - активировать её основной эффект каждый ход;
  - учитывать при проверке союзных способностей;
  - не отправлять в сброс вместе с кораблями.

- [x] Проверять количество торговли перед покупкой карты.

- [x] Не допускать отрицательный баланс торговли.

- [x] Проверять, что покупаемая карта действительно находится в торговом ряду
      или стопке исследователей.

- [x] Добавить возможность покупать `Explorer`.

- [x] Корректно обрабатывать окончание торговой колоды. Не добавлять
      `undefined` в торговый ряд, если новой карты нет.

## 2. Модель хода

- [x] Добавить статус партии:

  ```ts
  type GameStatus = 'waiting' | 'active' | 'finished';
  ```

- [x] Добавить номер или индекс текущего игрока.

- [x] Добавить номер хода.

- [x] Хранить и назначать единственного победителя партии через `isWinner`:

  - при создании партии устанавливать обоим игрокам `isWinner = false`;
  - разрешать назначить победителем только участника этой партии;
  - перед назначением сбрасывать `isWinner` у обоих игроков;
  - устанавливать `isWinner = true` только выбранному победителю;
  - назначать победителя и переводить партию в `finished` одной атомарной
    операцией `GameEntity.finishGame(...)`;
  - не допускать завершённую партию без победителя;
  - не допускать победителя у партии в статусе `waiting` или `active`;
  - запрещать повторное назначение или замену победителя завершённой партии;
  - не позволять контроллеру или сервису изменять `isWinner` напрямую;
  - сохранять и восстанавливать `isWinner` из БД.

- [x] Реализовать `startGame()`.

- [x] Реализовать `endTurn()`.

- [x] Вызывать `PlayerEntity.startTurn()` только из контролируемого перехода
      хода в `GameEntity` и не допускать повторной активации начала одного хода.

- [x] Проверять, что действие выполняет текущий игрок.

- [x] Запрещать игровые действия после завершения партии.

- [x] Реализовать полный цикл хода:

  1. начало хода;
  2. активация постоянных баз;
  3. розыгрыш карт;
  4. использование способностей;
  5. покупки и атаки;
  6. сброс неиспользованных карт;
  7. сброс разыгранных кораблей;
  8. обнуление торговли и боя;
  9. добор новой руки;
  10. переход хода.

- [x] В `finishTurn()` обнулять торговлю и бой.

- [x] Первый игрок в своём первом ходу должен получить 3 карты, второй — 5.

- [x] Корректно обрабатывать добор, если одновременно закончились личная колода
      и стопка сброса.

## 3. Зоны карт

- [x] Добавить отдельную зону для разыгранных кораблей:

  ```ts
  playedShips: DeckEntity;
  ```

- [x] Использовать `playedShips` для:

  - подсчёта разыгранных фракций;
  - союзных способностей;
  - `Stealth Needle`;
  - `Fleet HQ`;
  - `Blob World`;
  - отображения игрового стола;
  - сброса кораблей в конце хода.

- [x] При очистке зоны разыгранных карт сохранять существующий экземпляр
      `DeckEntity` через `currentPlayedCards.updateCards([])`, а не заменять его
      новым объектом. Это не позволит внешним ссылкам на зону устареть.

- [x] Добавить отдельную зону для утилизированных карт:

  ```ts
  scrappedCards: DeckEntity;
  ```

- [x] Различать:
  - утилизацию карты с руки;
  - утилизацию карты из сброса;
  - утилизацию самой разыгранной карты;
  - утилизацию карты торгового ряда;
  - уничтожение базы с перемещением в сброс.

## 4. Союзные способности

- [x] Реализовать `CardEntity.useMatchAbility()`.

- [x] Союзная способность должна становиться доступна при наличии другой карты
      той же фракции.

- [x] Порядок розыгрыша карт не должен мешать активации союза.

- [x] Базы, оставшиеся с предыдущих ходов, должны считаться союзниками.

- [x] Карта не должна активировать собственную союзную способность сама.

- [x] `Mech World` должен считаться союзником всех фракций.

- [x] `Stealth Needle` должен копировать свойства и фракцию выбранного корабля.

- [x] Союзный эффект одной карты должен активироваться не более одного раза за
      ход.

- [x] После розыгрыша карты пересчитывать доступность союзных способностей всех
      разыгранных карт и баз текущего игрока.

## 5. Идентификация и использование способностей

- [x] Не идентифицировать использованную способность только через
      `TCardAbilitiesNames`.

- [x] Добавить идентификатор способности, например:

  ```ts
  type AbilityId = 'primary' | 'ally' | 'scrap';
  ```

  Либо использовать уникальный ID способности внутри экземпляра карты.

- [x] В `CardEntity.useAbility()` проверять:

  - разыграна ли карта;
  - существует ли способность;
  - доступна ли она;
  - выполнены ли её условия;
  - не использована ли она ранее.

- [x] После использования устанавливать `ability.isUsed = true`.

- [x] Не добавлять одинаковые записи в `usedAbilities`.

- [x] Реализовать вызов:

  - обычных способностей;
  - союзных способностей;
  - утилизационных способностей.

- [x] Сбрасывать состояние использованных способностей в подходящий момент
      следующего хода.

- [x] В начале каждого хода снова делать доступными основные и союзные
      способности баз. Эффекты с выбором не выполнять автоматически — игрок
      должен активировать их сам.

- [x] Разделить состояния «карта находится в игре» и «карта разыграна в текущем
      ходу». Не использовать один `isPlayed` для обоих состояний базы.

## 6. Исполнение `Action`

- [x] Реализовать получение очков торговли.

- [x] Реализовать восстановление влияния.

- [x] Реализовать утилизацию карты с руки.

- [x] Реализовать утилизацию карты из сброса.

- [x] Реализовать выбор между рукой и сбросом.

- [x] Реализовать утилизацию карты торгового ряда с последующим заполнением
      торгового ряда.

- [x] Реализовать уничтожение выбранной базы.

- [x] Реализовать `CHOOSE_ONE`.

- [x] Реализовать `SEQUENCE`.

- [x] Реализовать условные действия.

- [x] Реализовать сброс карт и добор такого же количества.

- [x] Реализовать помещение следующего купленного корабля наверх личной колоды.

- [x] Реализовать бесплатное приобретение корабля наверх личной колоды.

- [x] Реализовать копирование корабля.

- [x] Реализовать универсального союзника всех фракций.

- [x] Реализовать получение боя за каждый разыгранный корабль.

- [x] Реализовать добор за карты указанной фракции.

- [x] Учитывать параметр `value` у сброса карт соперником, а не всегда добавлять
      только единицу.

## 7. Ожидающие выбора действия

Для способностей, требующих ответа игрока, добавить состояние ожидающего
действия:

```ts
type PendingAction =
  | { type: 'choose_card_to_scrap' /* параметры */ }
  | { type: 'choose_base_to_destroy' /* параметры */ }
  | { type: 'choose_ship_to_copy' /* параметры */ }
  | { type: 'choose_one' /* параметры */ };
```

- [x] Хранить `pendingAction` в состоянии партии.

- [x] Не разрешать несовместимые игровые команды, пока обязательный выбор не
      завершён.

- [x] Проверять, что выбранная карта или база соответствует ограничениям
      способности.

- [x] Поддержать отмену необязательных действий.

- [x] Сохранять `pendingAction`, чтобы партия корректно восстанавливалась после
      перезапуска сервера.

## 8. Атака, базы и аванпосты

- [x] Разрешить атаковать конкретную базу.

- [x] Хранить или вычислять текущую защиту экземпляра базы.

- [x] Уничтоженную базу перемещать в сброс владельца, сбрасывая у неё
      `isPlayed` и состояние использованных способностей, чтобы после добора её
      можно было разыграть повторно.

- [x] Запрещать атаку игрока и обычных баз, пока у него есть аванпост.

- [x] Разрешать специальным эффектам уничтожать выбранную базу независимо от её
      защиты.

- [x] Вычитать потраченные очки боя.

- [x] Не разрешать тратить больше боя, чем накоплено.

- [x] Не наносить автоматически весь накопленный бой игроку.

- [x] Завершать игру, когда влияние игрока становится меньше или равно нулю.

- [x] Не позволять влиянию или защите приводить состояние игры к некорректным
      значениям.

## 9. Сброс карт соперником

- [x] Применять накопленный `discardCardsCount` в начале следующего хода
      соответствующего игрока.

- [x] Суммировать несколько эффектов сброса.

- [x] Позволять сопернику самому выбирать сбрасываемые карты.

- [x] Создавать обязательный `pendingAction` для выбора карт.

- [x] Не разрешать продолжить ход, пока обязательный сброс не выполнен.

- [x] Если карт на руке недостаточно, сбрасывать доступное количество.

- [x] После выполнения обнулять `discardCardsCount`.

## 10. `DeckEntity`

- [x] Переименовать `updateCards()` так, чтобы было понятно, что метод полностью
      заменяет содержимое колоды.

- [x] Добавить явные операции:

  - удалить карту по ID;
  - взять верхнюю карту;
  - положить карту наверх;
  - положить карту в сброс;
  - проверить наличие карты;
  - безопасно заменить карту;
  - переместить карту между зонами.

- [x] Сделать перемещение карты между зонами атомарной операцией, чтобы один
      экземпляр не оказался сразу в двух местах.

- [x] Заменить `sort(() => Math.random() - 0.5)` на алгоритм Fisher–Yates.

- [x] Предусмотреть внедряемый или seed-based генератор случайных чисел для
      воспроизводимых тестов.

## 11. Сохранение состояния

- [x] Добавить в `CardFromDb`:

  - владельца карты;
  - зону карты;
  - позицию внутри зоны;
  - использованные способности;
  - временное состояние базы;
  - состояние копирования;
  - признак утилизации.

- [x] Добавить в состояние игрока:

  - разыгранные корабли;
  - базы;
  - ожидающие обязательные действия;
  - порядок хода;
  - состояние первого хода.

- [x] Добавить модели Prisma для Starfall:

  - партия;
  - участники;
  - экземпляры карт;
  - расположение и порядок карт;
  - текущее состояние партии;
  - победитель;
  - ожидающее действие;
  - версию состояния.

- [x] Добавить транзакционное сохранение всей игровой команды.

- [x] Добавить версию состояния или optimistic locking для защиты от
      параллельных запросов.

## 12. API и безопасность состояния

- [x] Реализовать команды API:

  - создать партию;
  - присоединиться к партии;
  - начать партию;
  - получить состояние;
  - разыграть карту;
  - использовать способность;
  - разрешить ожидающий выбор;
  - купить карту;
  - атаковать базу;
  - атаковать игрока;
  - закончить ход.

- [x] На каждой команде проверять:

  - личность игрока;
  - участие игрока в партии;
  - текущий ход;
  - статус партии;
  - ожидающее действие;
  - версию состояния;
  - допустимость цели.

- [x] Не возвращать игроку:

  - руку соперника;
  - порядок карт в колоде соперника;
  - порядок собственной закрытой колоды.

- [x] Создать отдельные DTO для публичного и приватного представления партии.

- [x] Предусмотреть идемпотентность команд или защиту от повторного запроса.

## 13. Тесты

- [x] Проверить правильный состав стартовых колод.

- [x] Проверить добор трёх карт первым игроком и пяти вторым.

- [x] Проверить отсутствие стартовых карт и исследователей в торговом ряду.

- [x] Запретить покупку при недостатке торговли.

- [x] Запретить повторный розыгрыш одной карты.

- [x] Проверить сохранение баз между ходами.

- [x] Проверить перемещение кораблей в сброс в конце хода.

- [x] Проверить союзную способность при разном порядке розыгрыша карт.

- [x] Проверить `Mech World` для каждой фракции.

- [x] Проверить копирование через `Stealth Needle`.

- [x] Проверить блокировку целей аванпостом.

- [x] Проверить перемещение утилизированной карты из игры.

- [x] Проверить перемешивание сброса после окончания личной колоды.

- [x] Проверить корректное уменьшение торгового ряда после окончания торговой
      колоды.

- [x] Проверить завершение игры при влиянии меньше или равном нулю.

- [x] Проверить восстановление партии из базы данных.

- [x] Проверить, что повторный API-запрос не применяет команду дважды.

## Рекомендуемый порядок реализации

1. Исправить создание партии и разделение колод.
2. Ввести явные зоны карт и безопасное перемещение между ними.
3. Реализовать жизненный цикл хода.
4. Реализовать базы, аванпосты и атаку.
5. Добавить идентификаторы способностей и союзную механику.
6. Добавить `pendingAction`.
7. Реализовать все типы `Action`.
8. Добавить сохранение в Prisma.
9. Добавить API и DTO представления состояния.
10. Покрыть доменную модель и API тестами.
